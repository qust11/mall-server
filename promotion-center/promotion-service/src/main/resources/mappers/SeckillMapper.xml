<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ym.promotion.mapper.SeckillMapper">
    <select id="countBySkuAndTime" resultType="java.lang.Integer">
        SELECT COUNT(0)
        FROM seckill s
        INNER JOIN promotion p ON s.promotion_id = p.id AND p.promotion_type = 2
        <where>
            s.is_deleted = 0
            AND p.is_deleted = 0
            AND sku_id = #{skuId}
            AND (p.start_time BETWEEN #{startTime} AND #{endTime} OR p.end_time BETWEEN #{startTime} AND #{endTime})
        </where>
    </select>
</mapper>