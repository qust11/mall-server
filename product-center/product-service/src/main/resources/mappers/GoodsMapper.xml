<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ym.product.mapper.goods.GoodsMapper">

    <select id="pageAdminProduct" resultType="com.ym.product.dto.resp.goods.spu.GoodsSpuListResp">
        SELECT
            gs.id,
            goods_name,
            spu_code,
            brand_name,
            brand_id,
            sub_title,
            tag_text,
            category_id,
            main_img
        FROM goods_spu gs
        LEFT JOIN goods_brand gb ON gs.brand_id = gb.id
        ORDER BY gs.id DESC
    </select>
</mapper>